---
date: 2026-05-21
---

# 2026-05-21 工作日志

> **定位**：当天工作日志。记录做了什么、发现了什么、结论是什么。

---

## 问题索引

| # | 问题 | 状态 | 关联 |
|---|------|------|------|
| 1 | lint 遗留项审计与补齐 | 已完成 | [详情](#1-lint-遗留项审计与补齐) |
| 2 | 孤儿资源检测与收窄 | 已完成 | [详情](#2-孤儿资源检测与收窄) |
| 3 | 向量语义去重 | 已完成 | [详情](#3-向量语义去重) |
| 4 | 定期巡检调度 | 已完成 | [详情](#4-定期巡检调度) |
| 5 | CLI --check 子项过滤 | 已完成 | [详情](#5-cli---check-子项过滤) |
| 6 | 文档同步（D-03/D-05/D-06） | 已完成 | [详情](#6-文档同步) |
| 7 | 移除 97-tasks.md | 已完成 | [详情](#7-移除-97-tasksmd) |

---

## 1. lint 遗留项审计与补齐

**背景**：lint 巡检模块（D-05）标注为"部分实现"，未实现项包括自动修复、定期巡检、内容冲突检测等。

**审计**：对比设计文档 D-05 与 `lint.py` 代码，发现 5 项遗留开发项：
1. 定期巡检（cron）— 未实现
2. 孤儿资源检测 — 未实现
3. 向量语义去重 — 未实现
4. `--check` 子项过滤 — CLI 未实现
5. D-05 文档与代码不一致

**处理**：逐项实现，全部完成。D-05 状态升级为✅已实现。

**关联**：`src/linglong/knowledge/lint.py`、`src/linglong/knowledge/lint_schedule.py`

---

## 2. 孤儿资源检测与收窄

**实现**：`lint.py` 新增 `check_orphans()` 方法：
- 遍历所有实体，解析 `[[target]]` 引用
- 统计每个实体被引用次数
- 从未被引用的标记为孤儿

**问题**：首次运行发现 336 个孤儿（92.5%），因为知识库以独立文档为主（experience/source），几乎不使用 `[[链接]]` 语法。

**收窄**：将检测范围限制为 `concept` 和 `entity` facet（知识网络节点），其余 facet 是独立文档不需要被引用。

**增强**：检测到孤儿后，扫描其他文档是否提到该标题，输出引用建议：
```
[orphan] 0912359d... 孤儿资源：未被任何页面引用，20 篇文档提到此标题
        → 建议在 1430b1c8... (linglong-pipeline) 中添加 [[链接]]
```

**结果**：孤儿从 336 降至 14 个，都是有价值的引用建议。

**关联**：`src/linglong/knowledge/lint.py`、`src/linglong/cli.py`

---

## 3. 向量语义去重

**实现**：增强 `check_content_conflicts()`：
- 遍历每个实体，调用 `search_similar`（内容截断 500 字符）
- 余弦距离 < 0.1（相似度 > 0.95）标记为语义重复
- `diary`/`task-record` 目录豁免
- `try/except` 降级：embedding 不可用时只保留标题去重

**模型改动**：`Entity` 新增 `distance: float | None = None` 临时字段，`_row_to_entity` 安全提取（不影响数据库写入）。

**关联**：`src/linglong/knowledge/lint.py`、`src/linglong/core/models.py`、`src/linglong/knowledge/store.py`

---

## 4. 定期巡检调度

**实现**：新增 `lint_schedule.py` 模块：
- `parse_cron_to_schedule()` — 解析 `分 时 * * *` 格式的 cron 表达式
- `run_lint_and_log()` — 执行一次巡检并追加写入 `~/.linglong/logs/lint-schedule.log`
- `run_daemon()` — 守护进程模式，先执行一次全量巡检，再按 schedule 循环等待，支持 SIGTERM/SIGINT 优雅退出

**CLI 入口**：
- `linglong lint --daemon` — 后台守护进程模式
- `linglong lint --run-scheduled` — 单次执行（用于系统 cron）

**配置**：`init.py` 交互式初始化新增定时巡检问题，`.linglong.yaml` 新增 `lint_schedule` 字段。

**关联**：`src/linglong/knowledge/lint_schedule.py`、`src/linglong/cli.py`、`src/linglong/core/config.py`

---

## 5. CLI --check 子项过滤

**实现**：CLI 新增 `--check` 参数，支持 5 个值：
- `index` — 索引一致性
- `links` — 死链检测
- `conflicts` — 标题重复 + 语义去重
- `stale` — 过期内容
- `orphans` — 孤儿资源

保留 `--rule` 参数向后兼容，标注为"已废弃，请使用 --check"。

**关联**：`src/linglong/cli.py`

---

## 6. 文档同步

**审计**：对比 10 份设计文档与代码，发现 3 份文档滞后：

| 文档 | 滞后项 | 处理 |
|------|--------|------|
| D-03 写入设计 | 语义去重、归档批量操作已实现但标注未实现 | ✅ 更新 |
| D-05 巡检设计 | 定期巡检已实现但标注未实现，流程图过时 | ✅ 更新 |
| D-06 Agent 接入 | MCP Server 已上线但关联实现未补齐 | ✅ 更新 |

**D-05 流程图调整**：
- 触发方式去掉"（可选）"，补上实际 CLI 参数
- 检查项标注 `--check` 对应参数
- 内容冲突拆分为标题重复（⚠️黄灯）和语义重复（ℹ️信息）
- 新增检查 5：孤儿资源（仅 concept/entity）
- 严重度从三级改为四级（红/黄/蓝信息/绿）

**关联**：`docs/knowledge/design/03-write-path.md`、`05-lint.md`、`06-agent-integration.md`

---

## 7. 移除 97-tasks.md

**背景**：`97-tasks.md` 和 `journal/` 职责重叠。tasks 需要额外维护（任务完成打勾、新任务加入），经常忘记更新。journal 是自然工作流（每天写日志）。

**决策**：以 `journal/` 为每日工作基准，删除 `97-tasks.md`。所有任务已完成，无需迁移。

**保留**：`98-backlog.md`（长期规划）、`99-milestones.md`（阶段方向）。

**关联**：`docs/knowledge/tracking/97-tasks.md`（已删除）
