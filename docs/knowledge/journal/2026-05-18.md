---
date: 2026-05-18
---

# 2026-05-18 工作日志

> **定位**：当天工作日志。记录做了什么、发现了什么、结论是什么。
> **不记录**：任务计划（去 `../tracking/97-tasks.md`）、长期规划（去 `../tracking/98-backlog.md`）、阶段方向（去 `../tracking/99-milestones.md`）。

> 检查知识库同步输出质量（OpenClaw wiki → ~/linglong/wiki）

---

## 问题索引

| # | 问题 | 状态 | 解决前猜测 | 实际原因 | 关联 |
|---|------|------|------------|----------|------|
| 1 | Entity Facet 为空 | 已解决 | 同步 bug / 映射缺失 | 原始 wiki 无 entity 数据 | [详情](#1-entity-facet-为空) |
| 2 | 34 个文件数据库无记录 | 待处理 | 同步中断 / 手动复制 | 待确认 | [详情](#2-34-个文件数据库无记录) |
| 3 | 16 个无 slug 文件名 | 待处理 | title 提取失败 | 待确认 | [详情](#3-16-个无-slug-文件名) |
| 4 | 364 个 wikilinks 死链 | 待处理 | 目标未同步 | 待确认 | [详情](#4-364-个-wikilinks-死链) |
| 5 | 216 个 light sleep 重复 | 观察 | 无去重策略 | 正常，等 BACKLOG-001 | [详情](#5-216-个-light-sleep-重复) |
| 6 | Journal 格式确定 | 已解决 | — | 按天一篇，索引+详情结构 | [详情](#6-journal-格式确定) |

---

## 1. Entity Facet 为空

**现象**：
- 知识库统计 `entity: 0`
- `~/linglong/wiki/entity/` 目录为空

**调查过程**：
1. 检查原始 wiki `~/.openclaw/workspace/memory/wiki/entity/` — 目录不存在
2. `grep -r "^type: entity"` — 0 条结果
3. 检查同步适配器 `TYPE_TO_FACET` 映射表 — `"entity": EntityFacet.ENTITY` 已配置
4. 确认默认分支 — 未映射类型默认走 `SOURCE`

**关键数据**：
原始 wiki 类型分布：project(32), source-code-note(29), experience(15), concept(11), methodology(6)...

**结论**：原始 wiki 中本来就没有 entity 类型的文件，不是同步 bug。

**关联**：`src/linglong/knowledge/sync/openclaw.py:64-103`

---

## 2. 34 个文件数据库无记录

**现象**：`linglong lint` 报告 `index_consistency` 34 个：
- `concept/b3356305-wiki-目录结构设计.md`
- `concept/16794a25-概念：agent-架构设计.md`
- `concept/f70c0481-博客文章格式规范.md`
- ... 等 34 个文件

**推测原因**：这些文件可能是手动放入 wiki 目录，未经过 `store.create()` 写入数据库。

**下一步**：运行 `linglong lint --fix` 看能否自动修复，或手动重新同步。

---

## 3. 16 个无 slug 文件名

**现象**：文件名只有 16 位 ID，无可读 slug：
- `experience/task-record/09d5c83db87fb6e6.md`
- `personal/diary/1ce3daba1b0be913.md`

**推测原因**：原始文件无 title frontmatter，或 title 提取失败。

**下一步**：检查这些文件的内容，确认 title 缺失还是提取逻辑有 bug。

---

## 4. 364 个 wikilinks 死链

**现象**：大量 `[[目标]]` 指向的文件在知识库中不存在。

**典型死链**：
- `[[推理层]]` — 目标文件可能被误分类
- `[[../openclaw-source-analysis/index.md]]` — 相对路径，同步后失效
- `[[ORCHESTRATOR_SPEC]]`、`[[AGENT_COORDINATION]]` — OpenClaw 内部标识

**分类**：
- 概念链接（约 50 个）— 目标文件可能未同步或被误分类
- OpenClaw 内部标识（约 100 个）— 原始 wiki 的术语表链接
- 相对路径链接（约 30 个）— 同步时路径未转换
- 其他（约 184 个）— 待进一步分类

**下一步**：分批处理，先修概念链接，再处理相对路径。

---

## 5. 216 个 light sleep 重复

**现象**：`content_conflict` 报告 216 个 `light sleep` 标题重复。

**原因**：memory 同步时，同一个任务每次会话产生一条新记录，无 content hash 去重。

**处理**：等 BACKLOG-001（同步去重策略）解决，当前不影响使用。

---

## 6. Journal 格式确定

**决策**：新建 `docs/knowledge/journal/` 目录，按天一篇记录工作日志。

**格式**：
- 顶部：问题索引表格（标题、状态、猜测、原因、关联）
- 下面：每个问题的详情 section
- 适中详细程度：有结论、有关键数据、有关联链接

**关联**：`docs/knowledge/journal/README.md`
