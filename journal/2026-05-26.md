---
date: 2026-05-26
---

# 2026-05-26 工作日志

> **定位**：当天工作日志。记录做了什么、发现了什么、结论是什么。

---

## 计划

| # | 任务 | 模块 | 优先级 | 状态 |
|---|------|------|--------|------|
| 1 | ingest MCP 接入 Claude Code | mcp | **高** | 待开始 |
| 2 | ingest MCP 接入 OpenClaw | mcp | **高** | 待开始 |
| 3 | 端到端验证：两个 Agent 调用 ingest 工具 | integration | **高** | 待开始 |

---

## 1. ingest MCP 接入 Claude Code

**目标**：Claude Code 已在 `~/.claude/mcp.json` 配置了 linglong MCP server，验证新增的 `generate_brief` 和 `search_web` 工具可用。

**步骤**：
1. 确认 `~/.claude/mcp.json` 中 linglong server 配置正确
2. 在 Claude Code 对话中调用 `generate_brief`，验证早报生成
3. 调用 `search_web("AI news")`，验证搜索结果返回
4. 确认 API Key 认证通过（SearXNG/RSSHub/Embedding）

**前置条件**：环境变量 `SEARXNG_API_KEY` / `RSSHUB_ACCESS_KEY` / `EMBEDDING_API_KEY` 已在 `~/.bashrc` 设置。

---

## 2. ingest MCP 接入 OpenClaw

**目标**：让 OpenClaw Gateway 通过 MCP 调用 linglong 的 ingest 工具。

**步骤**：
1. 确认 OpenClaw MCP client 配置方式（~/.openclaw/config.yaml 或 Gateway 配置）
2. 添加 linglong MCP server（stdio 模式：`python -m linglong.mcp`）
3. OpenClaw 对话中调用 `generate_brief` / `search_web`
4. 验证 OpenClaw 能读取 linglong 知识库（search_wiki/write_entity 已可用）

**注意**：
- OpenClaw 需要能访问 linglong venv 中的 Python
- linglong MCP server 启动时需要环境变量已加载
- 如果 OpenClaw 跑在服务器上，需要服务器也部署 linglong 或用 HTTP 模式

---

## 3. 端到端验证

**验证清单**：
- [ ] Claude Code 调用 `generate_brief` 成功返回早报
- [ ] Claude Code 调用 `search_web("AI news")` 返回搜索结果
- [ ] OpenClaw 调用 `generate_brief` 成功返回早报
- [ ] OpenClaw 调用 `search_web` 返回搜索结果
- [ ] 两个 Agent 调用 `write_entity` 写入知识库正常
- [ ] API Key 认证全链路通过
