---
date: 2026-05-22
---

# 2026-05-22 工作日志

> **定位**：当天工作日志。记录做了什么、发现了什么、结论是什么。

---

## 问题索引

| # | 问题 | 模块 | 优先级 | 状态 | 关联 |
|---|------|------|--------|------|------|
| 1 | 推送待提交代码 c6b946c | git | 中 | 待处理 | [详情](#1-推送待提交代码) |
| 2 | BACKLOG-005 facet 分类体系审核 + 重分类方案 | knowledge | **高** | 待处理 | [详情](#2-facet-分类体系审核) |
| 3 | ingest 解耦 KnowledgeStore | ingest | 中 | 待处理 | [详情](#3-ingest-解耦-knowledgestore) |
| 4 | 移除 pipeline deprecated 别名残留 | cli | 低 | 待处理 | [详情](#4-移除-pipeline-deprecated-残留) |
| 5 | IngestAdapter → KnowledgeAdapter 重命名 | composer | 低 | 待处理 | [详情](#5-ingestadapter--knowledgeadapter) |
| 6 | knowledge DB 新增 output_log 表 | knowledge | 中 | 待处理 | [详情](#6-output_log-表) |
| 7 | ingest 暴露为 MCP 工具 | mcp | 中 | 待处理 | [详情](#7-ingest-mcp-工具) |

---

## 1. 推送待提交代码

**问题**：上次会话提交 `c6b946c`（docs: PROJECT_OVERVIEW update）因代理挂了未推送到远端。

**计划**：检查远端状态，如未推送则 `git push`。

---

## 2. facet 分类体系审核

**问题**：94/142 条实体全在 source facet，OpenClaw 同步时映射粒度不够细。需要：
1. 审核 7 个 facet 分类（concept/experience/synthesis/source/api/decision/project_overview）是否合理
2. 设计 LLM 辅助重分类方案
3. 用户确认新分类目录后执行

**状态**：⏳ 等待用户审核分类体系

---

## 3. ingest 解耦 KnowledgeStore

**问题**：`rss.py:104` 和 `executor.py:22` 仍依赖 KnowledgeStore，ingest 应只返回数据给调用方。

**计划**：移除 store 参数，ingest 方法改为返回数据而不写入。

---

## 4. 移除 pipeline deprecated 残留

**问题**：`cli.py:800-802` 仍有 deprecated 别名，pipeline 子命令应完全移除。

**计划**：删除 deprecated 注册代码，保留顶级命令。

---

## 5. IngestAdapter → KnowledgeAdapter

**问题**：composer 中 IngestAdapter 名称与 ingest 模块混淆，应改为 KnowledgeAdapter（数据来自知识库而非 ingest）。

**涉及文件**：`composer/ingest_adapter.py` → 重命名 + `composer/composer.py` 引用更新。

---

## 6. output_log 表

**问题**：composer→dispatch 发布后无追踪，可能重复消费同一 entity。

**计划**：knowledge SQLite 新增 `output_log` 表：
```sql
CREATE TABLE output_log (
    id INTEGER PRIMARY KEY,
    entity_id TEXT NOT NULL,
    article_title TEXT,
    publisher TEXT NOT NULL,
    published_at TEXT NOT NULL
);
```

---

## 7. ingest MCP 工具

**问题**：Agent 无法通过 MCP 调用 ingest 采集信息。

**计划**：在 MCP Server 注册 ingest 相关工具（如 `fetch_rss`、`execute_package`），让 Agent 可按需采集。
