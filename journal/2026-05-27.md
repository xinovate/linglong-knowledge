---
date: 2026-05-27
---

# 2026-05-27 工作日志

> **定位**：当天工作日志。记录做了什么、发现了什么、结论是什么。

---

## 计划

| # | 任务 | 模块 | 优先级 | 状态 |
|---|------|------|--------|------|
| 1 | 提交推送 composer → reviewer 重构 | 全局 | **高** | ✅ 完成 |
| 2 | 全局清理 composer 过时引用（15 文件） | 全局 | **高** | ✅ 完成 |
| 3 | dispatch README 清理未实现功能描述 | docs | **中** | ✅ 完成 |
| 4 | MCP 双路径路由：按模块拆分 HTTP 端点 | mcp | **高** | ✅ 完成 |
| 5 | 部署配置：nginx + systemd | deploy | **高** | ✅ 完成 |

---

## 完成记录

### 1. Composer → Reviewer 重构提交推送

- 提交 `3c8f080` refactor: 删除 composer 模块，新建 reviewer 评审模块
- 提交 `e99bc79` docs(testing): 移除 composer 引用，替换为 reviewer 模块

### 2. 全局清理 composer 过时引用

- **修复关键 bug**：`core/llm/__init__.py` import 仍然指向 `composer.llm`（运行时会 ImportError）
- 15 个文件修复：README.md、.linglong.example.yaml、docs/rules.md、docs/operations.md、docs/doc-map.yaml、docs/dispatch/README.md、docs/dispatch/blog-style.md、docs/core/config.md、docs/ingest/design/00-overview.md、docs/knowledge/design/03-write-path.md、cli.py、models.py、manager.py、init.py、test_dispatch_manager.py
- 提交 `b823b18` docs: 全局清理 composer 过时引用

### 3. Dispatch README 清理

- 移除未实现的"发布队列"状态机、output_log、反馈收集描述
- 只保留实际实现：内容路由、OSS CDN、Hexo/Local 发布器
- 提交 `f5f6751` docs(dispatch): 清理未实现功能的描述

### 4. MCP 双路径路由

**做了什么**：

- `server.py` 新增 `create_http_app()`：按 enabled_modules 为每个模块创建独立 FastMCP 实例，各自路由 `/mcp/{module}`
- `__main__.py` HTTP 模式改用 `create_http_app()` + Token 认证中间件
- `create_server()` 保持不变（stdio 模式用）
- 远程配置 `enabled_modules: [ingest]` → 只暴露 `/mcp/ingest`
- 本地配置 `enabled_modules: [knowledge, reviewer]` → stdio 模式

**验证**：
- 276 测试全通过
- 远程路由生成 `/mcp/ingest`（5 工具）
- 本地路由生成 `/mcp/knowledge`（9 工具）+ `/mcp/reviewer`（1 工具）
- stdio 模式不受影响

### 5. 部署配置

- `deploy/linglong-mcp.service` 更新：默认 `LL_MCP_ENABLED_MODULES=ingest`，HTTP 模式环境变量
- `deploy/nginx.conf` 新建：反向代理 + SSL + WebSocket upgrade + 300s 超时
