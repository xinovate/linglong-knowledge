---
date: 2026-05-27
---

# 2026-05-27 工作日志

> **定位**：当天工作日志。记录做了什么、发现了什么、结论是什么。

---

## 计划

| # | 任务 | 模块 | 优先级 | 状态 |
|---|------|------|--------|------|
| 1 | 提交推送 composer → reviewer 重构 | 全局 | **高** | ✅ 完成 |
| 2 | 全局清理 composer 过时引用（15 文件） | 全局 | **高** | ✅ 完成 |
| 3 | dispatch README 清理未实现功能描述 | docs | **中** | ✅ 完成 |
| 4 | MCP 双路径路由：按模块拆分 HTTP 端点 | mcp | **高** | ✅ 完成 |
| 5 | 部署配置：nginx + systemd | deploy | **高** | ✅ 完成 |
| 6 | 服务器部署 + SSL + Redis | deploy | **高** | ✅ 完成 |
| 7 | Cloudflare Tunnel 部署 | deploy | **高** | ✅ 完成 |

---

## 完成记录

### 1. Composer → Reviewer 重构提交推送

- 提交 `3c8f080` refactor: 删除 composer 模块，新建 reviewer 评审模块
- 提交 `e99bc79` docs(testing): 移除 composer 引用，替换为 reviewer 模块

### 2. 全局清理 composer 过时引用

- **修复关键 bug**：`core/llm/__init__.py` import 仍然指向 `composer.llm`（运行时会 ImportError）
- 15 个文件修复
- 提交 `b823b18` docs: 全局清理 composer 过时引用

### 3. Dispatch README 清理

- 移除未实现的"发布队列"状态机、output_log、反馈收集描述
- 提交 `f5f6751` docs(dispatch): 清理未实现功能的描述

### 4. MCP 双路径路由

- `server.py` 新增 `create_http_app()`：按 enabled_modules 为每个模块创建独立 FastMCP 实例
- `__main__.py` HTTP 模式改用 `create_http_app()` + Token 认证
- 修复 lifespan 合并 bug（AsyncExitStack）
- 修复 DNS rebinding protection（`TransportSecuritySettings` + `allowed_hosts`）
- 提交 `0840f15`, `c7e18f9`, `6022417`

### 5. 服务器部署

- Python 3.11 安装 + linglong 部署到 `/opt/linglong/`
- MCP 服务 systemd 守护（`linglong-mcp.service`）
- DNS A 记录 `your-domain.com → localhost`
- nginx HTTPS 反代 `/mcp/` → `127.0.0.1:9900`

### 6. SSL 证书

- acme.sh + ZeroSSL + 阿里云 DNS API 自动签发（绕过 ICP HTTP 拦截）
- 证书路径：`/etc/nginx/ssl/your-domain.com.crt`
- acme.sh cron 自动续期
- 服务器代理清除（`/etc/profile` 和 Docker daemon）

### 7. Redis 动态 Token 认证

- Docker `linglong-redis` 部署（127.0.0.1:6379）
- Token 格式：`linglong-{module}-{random}`，Redis key=value=active，无过期
- `_auth.py` 改为 Redis 优先 + 静态降级
- `MCPConfig` 新增 `redis_url` 字段
- 提交 `d0f5cc2`

### 8. Cloudflare Tunnel 部署

- 阿里云 ICP 拦截 443/8443 等端口的 HTTPS/HTTP（境外域名 `.wiki` 无法备案）
- DNS 迁移至 Cloudflare（NS: `ns.cloudflare.com` / `ns.cloudflare.com`）
- `cloudflared` 安装 + tunnel `linglong-mcp` 创建
- CNAME `your-domain.com` → `cfargotunnel.com`（Cloudflare 代理）
- systemd 服务 `cloudflared-mcp` 开机自启
- 关闭阿里云安全组 8443 端口（不再需要）
- `https://your-domain.com/mcp/ingest` 不带端口，直连成功

---

## 今日统计

| 指标 | 数值 |
|------|------|
| 提交数 | 10 |
| 测试数 | 276 |
| 文档更新 | 10+ 文件 |
| MCP 远程端点 | `https://your-domain.com/mcp/ingest` |
| Redis 容器 | `linglong-redis` (6379) |
| Cloudflare Tunnel | `cloudflared-mcp`（systemd 自启） |
| DNS | Cloudflare 托管（NS 迁移完成） |
