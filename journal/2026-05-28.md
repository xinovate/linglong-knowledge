# 2026-05-28

## Context

ingest 拆为 linglong-scout 后，reviewer/dispatch 也从 staged 改动中删除。项目只剩 knowledge + MCP。文档和代码大量滞后，需要全面清理。

## Phase 1: 死代码清理 + 文档重写

- [x] Fix missing redis dependency — pyproject.toml: 移除 Pillow/watchdog/playwright，新增 server 可选组
- [x] Remove dead code — 删除 core/llm/、Task/TaskStatus、watch_enabled、init.py 中 reviewer/dispatch 模板
- [x] Rewrite architecture.md / api.md — 重写为 knowledge-only
- [x] Sync PROJECT_OVERVIEW.md, CLAUDE.md, operations.md, roadmap.md, config.md, testing.md

## Phase 2: 配置重命名 + 文档扁平化

- [x] `.linglong.yaml` → `.knowledge.yml`（向后兼容搜索旧文件名）
- [x] 环境变量前缀 `LL_` → `KB_`
- [x] 默认数据目录 `~/linglong/` → `~/knowledge/`
- [x] 模板目录 fallback（新路径优先，旧路径兼容）
- [x] `.linglong.example.yaml` → `.knowledge.example.yml`（内容同步精简）
- [x] docs 目录扁平化：`docs/core/` / `docs/knowledge/{design,agents,references}/` → `docs/{config.md, knowledge.md, design/, agents/, references/}`
- [x] 23 个文档 + 4 个 rules 文件更新引用
- [x] 测试同步更新（config 路径、yaml 文件名、错误消息英文化）

## Result

167 tests passed。零残留旧引用（grep 验证 .linglong.yaml / ~/linglong/ / LL_ 均无结果）。

现有本地知识库不受影响：
- config.py 向后兼容搜索 `.linglong.yaml`
- templates.py fallback 到 `~/linglong/templates/`
- 已有 `.linglong.yaml` 配置文件仍可正常加载
